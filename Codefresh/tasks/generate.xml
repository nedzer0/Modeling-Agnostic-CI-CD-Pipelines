<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="Codefresh">
    <property name="ECLIPSE_HOME" value="../../.."/>
    <property name="ECLIPSE_WORKSPACE" value="../.."/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="Codefresh.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/Codefresh/bin"/>
    </path>

    <path id="Codefresh.classpath">
        <path refid="Codefresh.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="Codefresh.libraryclasspath.alljars">
        <pathelement location="../Codefresh/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="Codefresh.classpath.alljars">
        <path refid="Codefresh.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="Codefresh.main.Generate" classpathref="Codefresh.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="Codefresh.main.Generate" classpathref="Codefresh.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
