<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="Concourse">
    <property name="ECLIPSE_HOME" value="../../.."/>
    <property name="ECLIPSE_WORKSPACE" value="../.."/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="Concourse.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/Concourse/bin"/>
    </path>

    <path id="Concourse.classpath">
        <path refid="Concourse.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="Concourse.libraryclasspath.alljars">
        <pathelement location="../Concourse/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="Concourse.classpath.alljars">
        <path refid="Concourse.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="Concourse.main.Generate" classpathref="Concourse.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="Concourse.main.Generate" classpathref="Concourse.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
