<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="Drone">
    <property name="ECLIPSE_HOME" value="../../.."/>
    <property name="ECLIPSE_WORKSPACE" value="../.."/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="Drone.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/Drone/bin"/>
    </path>

    <path id="Drone.classpath">
        <path refid="Drone.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="Drone.libraryclasspath.alljars">
        <pathelement location="../Drone/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="Drone.classpath.alljars">
        <path refid="Drone.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="Drone.main.Generate" classpathref="Drone.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="Drone.main.Generate" classpathref="Drone.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
