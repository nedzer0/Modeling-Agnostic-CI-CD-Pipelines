<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="CircleCI">
    <property name="ECLIPSE_HOME" value="../../.."/>
    <property name="ECLIPSE_WORKSPACE" value="../.."/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="CircleCI.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/CircleCI/bin"/>
    </path>

    <path id="CircleCI.classpath">
        <path refid="CircleCI.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="CircleCI.libraryclasspath.alljars">
        <pathelement location="../CircleCI/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="CircleCI.classpath.alljars">
        <path refid="CircleCI.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="CircleCI.main.Generate" classpathref="CircleCI.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="CircleCI.main.Generate" classpathref="CircleCI.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
