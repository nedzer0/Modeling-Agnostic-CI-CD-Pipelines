<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="TravisCI">
    <property name="ECLIPSE_HOME" value="../../.."/>
    <property name="ECLIPSE_WORKSPACE" value="../.."/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="TravisCI.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/TravisCI/bin"/>
    </path>

    <path id="TravisCI.classpath">
        <path refid="TravisCI.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="TravisCI.libraryclasspath.alljars">
        <pathelement location="../TravisCI/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="TravisCI.classpath.alljars">
        <path refid="TravisCI.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="TravisCI.main.Generate" classpathref="TravisCI.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="TravisCI.main.Generate" classpathref="TravisCI.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
